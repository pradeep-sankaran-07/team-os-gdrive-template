# documents/

One file per item, `<yyyy-mm-dd>-<author-slug>-<kebab-topic>.md`. Append only.

Anything here that concerns a customer is also cross-posted into that account's
folder, so the customer record stays complete.
