# assets/

Templates, design tokens, images and scaffolds this skill needs at run time.

Keep it small. This folder is mirrored verbatim, and for any owner whose
`auto_promote` is `prose-only` a change here escalates to an admin rather than
promoting automatically, because assets ripple outward into other skills.
