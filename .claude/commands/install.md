---
description: Set up, continue or check the installation of this Team OS.
---
Run the `install-team-os` skill at `.claude/skills/install-team-os/SKILL.md`. $ARGUMENTS may name a phase or a specific step.

Read `INSTALL-CHECKLIST.md` first: it is the state. Report one step at a time. Never mark a step done until its probe in `references/verification.md` has actually passed, and never invent a value into `config.yaml`.
