---
description: Apply the Acme visual standard to a document, deck, sheet or chart.
---
Run the `acme-brand` skill at `.claude/skills/acme-brand/SKILL.md` for the output described in $ARGUMENTS.

Colour and type come from `assets/tokens/`, by token name where the format allows. Never redefine a value locally and never approximate a hex. If the logo file is not present, ask for it rather than producing an unbranded deliverable.

Boundary: this controls how it looks. What a public document may claim is `acme-marketing`. Answer both questions.
